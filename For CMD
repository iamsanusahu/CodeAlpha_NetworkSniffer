GENERAL SETUP (For Windows CMD)
1. Install Python (if not already installed)
Download: https://www.python.org/downloads

During installation:

✅ Check “Add Python to PATH”

Click Install Now

2. Open CMD or PowerShell as Administrator
🐍 Task 1: Basic Network Sniffer (Python + Scapy on Windows)
⚠️ Note:
Scapy works on Windows but needs Npcap to capture packets.

Step-by-step:
A. Install Npcap
Download and install Npcap:
🔗 https://nmap.org/npcap
During installation:

✅ Select “Install Npcap in WinPcap API-compatible Mode”

B. Install Scapy
cmd
Copy
Edit
pip install scapy
C. Create Python file
cmd
Copy
Edit
notepad sniffer.py

Paste this code and save:

python
Copy
Edit
from scapy.all import sniff, IP, TCP, UDP, ICMP

def packet_callback(pkt):
    if IP in pkt:
        ip_src = pkt[IP].src
        ip_dst = pkt[IP].dst
        proto = pkt[IP].proto
        info = f"{ip_src} → {ip_dst}   proto={proto}"
        if TCP in pkt:
            info += f" TCP sport={pkt[TCP].sport} dport={pkt[TCP].dport}"
        elif UDP in pkt:
            info += f" UDP sport={pkt[UDP].sport} dport={pkt[UDP].dport}"
        elif ICMP in pkt:
            info += f" ICMP type={pkt[ICMP].type}"
        payload = bytes(pkt[IP].payload)
        if payload:
            info += f" payload={payload[:20]!r}..."
        print(info)

print("Listening on all interfaces, Ctrl-C to stop...")
sniff(filter="ip", prn=packet_callback, store=False)
D. Run the script:
cmd
Copy
Edit
python sniffer.py
If needed, run CMD as administrator for permissions.
